<form class="section-add">
  <div class="input-group">
    <label class="input-group-addon">新建片段</label>
    <input type="text" class="form-control" name="name" placeholder="名称" autocomplete="off" value="">
  </div>
  {{#if id}}<input type="hidden" name="before" value="{{id}}">{{/if}}
  <button type="submit">保存</button>
</form>